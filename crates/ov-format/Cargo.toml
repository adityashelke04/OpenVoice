[package]
name = "ov-format"
description = "OpenVoice text formatting pipeline: voice commands, developer vocabulary, case transforms. Pure, no OS dependencies."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }

[dev-dependencies]
pretty_assertions.workspace = true

[lints.rust]
missing_docs = "warn"
unsafe_code = "forbid"

[lints.clippy]
all = { level = "warn", priority = -1 }
